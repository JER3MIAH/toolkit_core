name: toolkit_core
description: "Material 3 ready UI toolkit with themed widgets, inputs, navigation helpers, validators, and loggers for Flutter apps."
version: 0.2.0
# homepage:
repository: https://github.com/JER3MIAH/flutter_toolkit
issue_tracker: https://github.com/JER3MIAH/flutter_toolkit/issues

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

topics:
  - widgets
  - theming
  - navigation
  - forms
  - utilities
